# Real Estate Wholesale Platform - Netlify Environment Variables
# Copy these to Netlify Site Settings > Build & Deploy > Environment

# ==========================================
# FRONTEND CONFIGURATION (Netlify Build)
# ==========================================

# API Configuration - Point frontend to your deployed backend API
VITE_API_BASE_URL=https://api.yourdomain.com
VITE_ENVIRONMENT=production

# Build Configuration
VITE_VERSION=1.0.0

# Optional: Analytics & Monitoring
VITE_SENTRY_DSN=https://your-sentry-key@sentry.io/project-id
VITE_GA_TRACKING_ID=UA-XXXXXXXXX-X

# ==========================================
# FEATURE FLAGS - Enable/Disable Features
# ==========================================
VITE_ENABLE_LEAD_SCOUT=true
VITE_ENABLE_OFFER_GENERATOR=true
VITE_ENABLE_BUYER_MATCHER=true
VITE_ENABLE_NEGOTIATION_ASSISTANT=true
VITE_ENABLE_SEO_CONTENT=true

# ==========================================
# BUILD ENVIRONMENT (Set by Netlify)
# ==========================================
# These are automatically set, no need to configure:
# NODE_VERSION=18.17.0
# NODE_ENV=production
# NPM_VERSION=9.6.7

# ==========================================
# IMPLEMENTATION NOTES:
# ==========================================
# 
# 1. VITE_API_BASE_URL: 
#    - For local dev: http://localhost:8000
#    - For staging: https://api-staging.yourdomain.com
#    - For production: https://api.yourdomain.com
#
# 2. VITE_ENVIRONMENT:
#    - 'development' = local debugging (verbose logs)
#    - 'staging' = staging branch deployments
#    - 'production' = main branch deployment (optimized)
#
# 3. SENTRY_DSN (optional):
#    - For error tracking: sign up at sentry.io
#    - Leave blank to disable
#
# 4. Feature Flags:
#    - Set to 'true' to enable agent in UI
#    - Set to 'false' to hide from user interface
#
# 5. To set these in Netlify:
#    - Log in to netlify.com
#    - Site Settings > Build & Deploy > Environment
#    - Click "Edit variables"
#    - Add each variable manually or paste list
#    - Trigger "Deploy site" to rebuild with new env vars
